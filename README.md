# LQTA-D Article 2 — Collaborative Relational Time

Development branch for the successor manuscript to:

**Serialization Gauge and Conditioned-Hazard Obstructions for Finite Quantum-Jump Event Kernels**  
Journal of Mathematical Physics submission `JMP26-AR-02157`.

The current public `v1.0.0`/`v1.0.1` releases remain historical snapshots of the narrower manuscript **Temporal Domains Without a Global Clock**. This branch does not rewrite those releases or any frozen scientific certificate.

## Revised scope

The successor article is being broadened to cover the full post-predecessor theory rather than treating temporal domains as the primary object. The organizing chain is:

`CERT-009 governance -> CERT-010 relational-time/interface composition -> CERT-011 collaborative reconstruction -> CERT-012 dynamic coordination lifecycle -> CERT-013 semantic audit -> CERT-014 dynamic temporal atlas -> CERT-015 finite scale growth -> CERT-016 relational coherence with global-scalar deconditioning -> CERT-017 typed interaction/partial glue -> TC9A/B/C gauge-invariant field structure and continuity`.

The central working thesis is that finite collective temporal organization can be reconstructed, and in a precise collaborative sense generated, from overlapping local relational dependencies without requiring a primitive global scalar clock. Temporal domains are one intermediate layer of this construction, not the definition of the theory.

## Successor boundary

- `CERT-001` through `CERT-008` belong to the predecessor article line.
- `CERT-009` (`PASS_MASTER_SUCCESSOR_GOVERNANCE`) opens the nonretroactive successor program.
- `CERT-010` onward supplies the scientific and structural content of the successor theory.
- Historical FAILs, abandoned pre-science attempts, hashes, gates, and frozen certificates remain immutable.

See `docs/CERTIFICATION_LINEAGE.md` and `docs/ARTICLE2_SCOPE_REVISION.md`.

## Core conceptual layers

1. **Relational composition without primitive global serialization** — TIME-RED-1 / CERT-010.
2. **Collaborative temporal reconstruction** — TC1-TC3 / CERT-011, including collaborative complexity `kappa`.
3. **Dynamic coordination and lifecycle** — TC4 / CERT-012, separating historical precedence from mutable active coordination.
4. **Semantic separation and temporal atlases** — CERT-013 and TC5/CERT-014.
5. **Finite scale and scalar deconditioning** — CERT-015/016: relational coherence need not require uniformly well-conditioned global scalarization.
6. **Typed interaction and partial gluing** — TC8/CERT-017 with distinct ORD, CAL, and CORR semantics.
7. **Gauge-invariant calibration field structure** — TC9A/B/C: `gamma`, Hodge decomposition, `eta=P_perp gamma`, local density `rho`, provenance-selected current `J`, and `Delta rho + div_T J = S`.

## Claim boundary

The finite relational results do **not** establish:

- a universal theory of physical time;
- continuum spacetime;
- a preferred spatial geometry or dimension;
- Lorentz symmetry;
- gravity or Einstein dynamics;
- physical curvature from affine CAL holonomy;
- literal creation/destruction of physical time;
- a Noether origin for `Q_T` or its source-free balance.

Global scalarization is an optional integrable reduction, not the definition of time. Reconstructibility is not causal influence; active connectivity is not historical precedence; CORR does not imply ORD or CAL.

## Archived release and DOI

The historical narrower release remains archived at Zenodo DOI `10.5281/zenodo.21931500`. No Zenodo metadata or release is being overwritten from this development branch. A later successor version should be created only after the broadened manuscript title, scope, claim map, and reproducibility package are internally coherent.

## Repository map

- `manuscript/` — historical narrower manuscript until replaced on the development branch by the broadened draft.
- `figures/` — current manuscript figures.
- `evidence/certifications/` — frozen finite campaign certification packages.
- `evidence/foundations/` — exact structural/foundational packages.
- `docs/` — lineage, claim boundaries, reproducibility, and scope revision.
- `code/` — integrity/reproduction helpers.
- `licenses/` — code and documentation/data licences.
- `SHA256SUMS.txt` — integrity manifest for the archived release; not a manifest of ongoing branch edits.
