# Reproducibility

The archive intentionally preserves the successor certification lineage. Read `CERTIFICATION_LINEAGE.md` before interpreting individual packages: certificates are heterogeneous in scope, and preserved FAIL/abandonment records are part of the scientific history rather than evidence for the final claims.

## Integrity

Run:

```bash
python code/verify_sha256.py
```

from the repository root.

The script verifies every file listed in `SHA256SUMS.txt`.

## Evidence hierarchy

### CERT-010
Causally realizable relational-time/interface composition and frozen-domain reproduction.

### CERT-011
Finite unseen robustness/genericity of nontrivial collaborative temporal reconstruction.

### CERT-012
Temporal-domain merge/split/no-resurrection/fresh-reglue lifecycle qualification.

### CERT-013
Global exact finite pre-noise audit with nonretroactive semantic addenda.

### CERT-014
Noisy/dynamic temporal-atlas qualification with gauge-invariant normalized transition diagnostics.

### CERT-015
Finite scale-growth/macroscopic-coordinate compatibility in the frozen TC6H domain.

### CERT-016
Finite relational temporal coherence with global scalar deconditioning in the frozen TC7H domain.

### CERT-017
Finite typed temporal-domain interaction and partial-glue specificity in the frozen TC8H domain.

### TC8A
Exact finite structural foundations for typed temporal-domain interaction and partial gluing.

### TC9A
Exact discrete Hodge/cochain foundations:
`gamma = D0 s + D1^T psi + h`.

### TC9B
Gauge-invariant field observables and the no-go for interpreting raw `div gamma` as a physical source.

### TC9C
Provenance-aware transport and divergence:
`Delta rho + div_T J = S`.

## Important distinction

The exact TC9 results and the finite holdout campaign results are different kinds of evidence.
The repository does not combine them into a stronger universal claim.

## Figure audit

The repository figures are explanatory renderings, not additional scientific evidence. Their labels
are bound to the manuscript definitions: identical connectivity for typed ORD/CAL/CORR relations;
`gamma = D0 s + D1^T psi + h` for the finite registered-complex Hodge decomposition; and
`Delta rho + div_T J = S` for provenance-selected transport with the stated current sign convention.
