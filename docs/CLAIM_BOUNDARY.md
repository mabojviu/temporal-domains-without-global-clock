# Claim boundary — broadened successor article

## Historical boundary

`CERT-001` through `CERT-008` belong to the predecessor article **Serialization Gauge and Conditioned-Hazard Obstructions for Finite Quantum-Jump Event Kernels** (`JMP26-AR-02157`).

`CERT-009` (`PASS_MASTER_SUCCESSOR_GOVERNANCE`) marks the nonretroactive boundary of the successor program. It is a governance certificate, not by itself a temporal-physics result.

The successor scientific chain begins with TIME-RED-1 / `CERT-010` and continues through `CERT-017` and the TC9A/B/C exact foundations.

## Supported within declared finite domains

The combined successor corpus supports, with the qualification class appropriate to each item:

- relational-time/interface composition without requiring a primitive global scalar clock (`CERT-010`);
- nontrivial collaborative temporal reconstructibility from joined local participant views, with finite collaborative complexity `kappa` and qualified unseen-domain robustness (`CERT-011`);
- locally generated finite examples of collaborative complexity under local token/ORD microdynamics, without a global scalar-time oracle;
- separation of historical ORD/provenance from mutable active coordination, including split, persistence of historical memory, non-resurrection of automatic future influence, and fresh reglue (`CERT-012`);
- exact semantic separation of reconstructibility, causal influence, active connectivity, historical precedence, CAL holonomy, and scalarizability (`CERT-013`);
- dynamic/noisy temporal-atlas compatibility and affine-gluing/holonomy diagnostics within a frozen finite ensemble (`CERT-014`);
- finite scale-growth compatibility of collective affine coordination within the tested domain (`CERT-015`);
- preservation of relational temporal coherence even when global-scalar conditioning degrades in the declared finite scale domain (`CERT-016`);
- typed ORD/CAL/CORR interaction and partial gluing whose semantics depend on relation type and consistency rather than connectivity alone (`CERT-017`);
- gauge-covariant affine calibration data `gamma`, discrete Hodge structure, and gauge-invariant circulation/holonomy observables (TC9A);
- transverse gauge-invariant content `eta=P_perp gamma`, invariant quadratic content `E_inv=||eta||^2`, and associated local density `rho` (TC9B);
- provenance-selected gauge-invariant transport current and exact finite continuity/balance equation `Delta rho + div_T J = S` (TC9C);
- on closed provenance patches, the algebraic balance `Delta Q_T = sum_i S_i`, with `Q_T=sum_i rho_i=||eta||^2`; source-free redistribution therefore preserves `Q_T` exactly.

## Interpretive thesis permitted

Within the explicitly finite and qualified setting, the results support the interpretation that collective temporal organization need not be introduced through one primitive global scalar clock. Overlapping local dependencies can support partially shared temporal structure and, in the qualified collaborative constructions, collective temporal information can require multiple local views jointly.

Temporal domains are active coordination patches appearing at an intermediate level of the construction. They are not assumed to be fundamental objects and are not the definition of relational time.

Global scalarization is an optional integrable reduction when consistency conditions permit it.

## Explicitly not supported

The successor corpus does **not** establish:

- a universal theorem of physical time emergence;
- continuum spacetime;
- a preferred spatial geometry;
- Euclidean, hyperbolic, spherical, Lorentzian, or other primitive metric geometry;
- a derived spatial or spacetime dimension;
- Lorentz invariance;
- gravitation or an Einstein equation;
- equivalence between CAL holonomy and physical/spacetime curvature;
- microcausality as a universal consequence of the present constructions;
- universal unravelling independence;
- literal creation or destruction of physical time;
- a fundamental conservation law for temporal content;
- a Noether derivation of `Q_T`.

## Mandatory semantic non-identifications

- reconstructibility != causal influence;
- incomparability != operational independence;
- active connectivity != historical precedence;
- active connectivity != directed reachability;
- historical memory != future influence;
- CORR != ORD;
- CORR != CAL;
- CAL holonomy != ORD cycle;
- affine CAL holonomy != spatial/gravitational curvature;
- scalarizability != definition of time.

## Geometry neutrality

No primitive coordinates, distances, angles, volumes, dimensions, or spatial embeddings are assigned by the successor theory.

Registered CAL 2-cells are compositional consistency cells, not spatial faces. The unit-weight cochain inner product used in finite Hodge constructions is an auxiliary combinatorial choice and is not interpreted as a physical spacetime metric.
